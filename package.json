{
  "name": "corujao-chat",
  "version": "1.0.0",
  "description": "Chat em tempo real com Socket.IO e MongoDB",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p public && echo '✅ Build completo!'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "connect-mongo": "^5.0.0",
    "crypto": "1.0.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "mongoose": "^8.0.3",
    "socket.io": "^4.7.2",
    "uuid": "^11.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "render": {
    "env": "node",
    "predeploy": "npm install"
  }
}
